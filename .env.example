# Repository in owner/name format
GITHUB_REPO=codedspaces/demo-2

# Optional GitHub token for higher rate limits (classic or fine-grained with repo read)
# GITHUB_TOKEN=ghp_xxx

# Poll interval in ms
POLL_MS=15000